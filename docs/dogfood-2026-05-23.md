# Nomos prod dogfood — 2026-05-23

End-to-end live test of the platform via the `@auto-nomos/mcp-server`
surface (Claude Code wiring) plus direct PDP/CP probes and the Python
SDK. Goal: validate every advertised integration before announce.

## Result

| Layer            | Pass | Fail | Notes |
|------------------|------|------|-------|
| deploy-smoke     | 17   | 0    | Baseline contract probes |
| Tier 1 (PDP direct, MCP-equiv) | 14 | 0 | First run 11/12; after matcher fix + slack write probe added: 14/14 |
| Tier 1 — discord (post-restart) | 1 | 0 | `/discord/channel/list` against OSS guild → 200, 19 channels |
| Python SDK (unit)| 16   | 0    | |
| Python SDK (prod e2e) | 2 | 0    | github_user_read + notion_search via SDK |
| Audit chain      | 15 receipts inspected, all `allow` for probe commands |
| **Tier 0 — MCP tools (post-key-rotation)** | **6** | **0** | After patching `~/.claude.json` `CB_API_KEY` → dogfood + killing stale MCP procs: `nomos_status`, `github_get_user`, `github_list_repos`, `github_create_issue` (issue #4), `notion_search`, `slack_list_channels`, `slack_post_message` (Slack 200 with `not_in_channel`), `github_close_issue` correctly returned `step_up_required` on second call (first call hit policy-cache lag). `google_list_files` → SDK fail-closed (`pdp_unreachable`) because PDP returns HTTP 502 wrapping the `oauth_token_invalid` upstream error — re-link Google to unblock. |

## Per-provider matrix

| Provider | Read | Write | Cosigner gate | Upstream note |
|----------|------|-------|---------------|---------------|
| github   | ✅ get_user, list_repos, list_issues | ✅ create_issue (201) | ✅ delete_repo → step_up_required | OAuth scope: repo, read:user |
| google (drive) | ⚠️ allow @ PDP — pre-fix upstream 404; post-fix upstream returns `oauth_token_invalid` because the OAuth access_token expired 2026-05-13 and refresh failed silently | n/a | n/a | PDP base fix (#2) is live; **user must re-link Google in dashboard** to mint fresh token |
| notion   | ✅ search, list_users, get_bot_user | not tested | not tested | api_base correct |
| slack    | ✅ list_channels, list_users | ✅ post_message (200) to #social | ✅ delete_message → step_up_required | api_base correct |
| discord  | ✅ list_channels via PDP-direct (19 channels in OSS guild) | step-up — blocked on auto-approve | n/a | guild 1507638058311221298 |
| azure / fs / ssh | not run — Tier 2 harness blocks on step-up (no auto-approve path on VM) | | | |

## Bugs found + fixed

| # | Severity | File | Commit | Status |
|---|----------|------|--------|--------|
| 1 | P1 | `scripts/{simulate-approval,e2e-sprint3,e2e-sprint5,demo,demo-dynamic-scope,demo-dynamic-scope-github,gen-keys,setup-wizard}.mts` — stale `@credential-broker/*` imports after rebrand | `bab82cc` | committed, no deploy needed (script-side only) |
| 2 | P0 | `apps/pdp/src/adapters/oauth.ts` — google base missing `/drive/v3` | `0dfdd76` | **awaiting VM redeploy** |
| 3 | P1 | `apps/control-plane/src/routes/agent-me.ts` — `/v1/agent/me/tools` filtered out multi-provider policies (integrationId IS NOT NULL) so MCP server got `integrations: []` | `1e34d83` | **awaiting VM redeploy** |
| 4 | P1 | `packages/sdk-python/src/nomos/guard.py` — proxy result `upstream_body` always None (read `payload.body` instead of `payload.upstream.body`) | `8525929` | **awaiting nomos-sdk PyPI republish** |
| 5 | P2 | `/Users/varendra/.cb-mcp.json` had stale api key `cb_27d49855…` + localhost URLs (pre-prod migration) → MCP tool calls returned `unauthorized` | n/a | **fixed in user dotfile**: now apiKey=dogfood, pdpUrl=https://pdp.auto-nomos.com, controlPlaneUrl=https://api.auto-nomos.com, integrations=[github,google,notion,slack]; user must restart Claude Code to pick up new config |

## Issues found, not yet fixed

| # | Severity | Symptom | Suggested fix |
|---|----------|---------|---------------|
| 6 | P2 | `prod-fs-mutate.mts` + `prod-discord-mutate.mts` + `prod-stepup-flow.mts` block on step-up because the dashboard passkey is the only approve surface, and `simulate-approval.mts` can't run on the VM without sudo over SSH | Either (a) add `NOMOS_AUTO_APPROVE=1` mode that flips state via DB on agents.update permission, or (b) document a "deploy a one-shot approve cronjob" recipe for CI. |
| 7 | P2 | Google sub-products (gmail/calendar/docs/sheets/tasks/contacts) have schema-pack packs + adapter YAMLs but no entries in `PROVIDER_API` — calls would route through the `google` connector (drive) and hit wrong base | Add per-pack `PROVIDER_API` entries with each product's `/v<ver>` suffix; introduce `connector` field on schema-packs so multiple google sub-bases coexist with one OAuth connection |
| 9 | P2 | PDP wraps upstream `oauth_token_invalid` in HTTP 502, so the SDK (TS + Python) maps it to `pdp_unreachable` (fail-closed) — user sees a generic "broker down" instead of "re-link your Google account" | Return 200 with `decision.reason='oauth_token_invalid'` (or 401, not 5xx) so SDK fail-closed gate doesn't trip on a user-actionable upstream failure. Affects every connector whose access token expired since last refresh. |
| 10 | P3 | PDP policy bundle cache lags ~tens-of-seconds behind `policies.upsert` writes — first call after a Cedar update can see old decisions (observed during MCP `github_close_issue` probe: allowed once, then correctly `step_up_required` on retry) | Shorten the cache TTL, or have `policyInvalidator.invalidate(customerId)` block on a PDP ack before the tRPC mutation returns. |
| 8 | P3 | `audit.list` rows return `createdAt = ?` (date wasn't serialized) when accessed via curl | likely a tRPC superjson wrapper missing in the curl payload — UI works fine |

## Post-restart status (this session)

- `~/.cb-mcp.json` rewritten to prod endpoints + dogfood key + 4 integrations. User must restart Claude Code for new MCP server process to pick up the config; `mcp__nomos__*` tool calls in *this* turn still hit the in-memory stale config.
- Tier 1 re-run after VM deploy: 14/14 PASS — confirms PDP google fix (#2) landed (upstream is now `oauth_token_invalid` not `404` → path resolves, only token expired).
- Direct discord probe: 200 OK against guild `1507638058311221298`.

## Awaiting verification

After user restarts Claude Code AND re-links the Google OAuth in the dashboard:

```bash
NOMOS_API_KEY=$(cat /tmp/dogfood_key) NOMOS_ORG_ID='ed539890-8bab-4d3d-804a-b1bfe8c90a9f' \
  GITHUB_TEST_REPO='admin-brickexchange/smoke-test-01' SLACK_TEST_CHAN='C0AFQKNQDM1' \
  pnpm tsx scripts/dogfood-tier1.mts
```

Expected: `google_list_files (read)` upstream=200, `google_storage_quota (read)` upstream=200.

Then verify the MCP tool surface works in a fresh Claude Code chat by calling
`mcp__nomos__github_get_user`, `mcp__nomos__notion_search`,
`mcp__nomos__slack_list_channels`, `mcp__nomos__google_list_files`.

For step-up auto-approve: the rebrand-stale import fix (#1) needs to reach the deployed
checkout on nomos-vm — once `/opt/nomos/app` is at a commit ≥ `bab82cc`,
`sudo pnpm tsx /opt/nomos/app/scripts/simulate-approval.mts <approvalId>` will work.

## Bootstrap state used

- Org `ed539890-8bab-4d3d-804a-b1bfe8c90a9f` (Nomos-dev-testing)
- Agent `bf709287-…` (claude-bot)
- Policy `83f3d5ce-…` (claude-bot-dogfood) — scoped: 4 providers, write-permit, destructive cosigner-gated
- Dogfood api key id `5485f5e1-…` (admin role)
- OAuth: github + slack + google (drive.readonly) + notion + discord — all live
